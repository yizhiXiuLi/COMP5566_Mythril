### **T1: Reentrancy (重入漏洞)**

- **合约：** `etherstore.sol` 。
- **失败原因：** **编译器断代。** 0.8.19 编译器强制要求 `constructor` 关键字和严格的溢出检查，看到 0.4.x 的 `constant` 状态变量直接罢工。
- **解决方案：** 在 `docker run` 命令中强制添加 `--solv 0.4.25` 参数，手动指定兼容的低版本编译器，绕过 Mythril 的自动识别机制。

### **T2: Arithmetic (算术溢出)**

- **合约：** `BECToken.sol`。

- **失败原因：** **路径爆炸 (Path Explosion)。** 该合约的 `batchTransfer` 涉及循环和多变量数学论证，导致 Z3 约束求解器在 $2^{256}$ 空间内计算超时，CPU 满载。

- **解决方案：** 1.  引入参数调优：设置 `--execution-timeout 300` 强制截断分析。

  \2.  **方法论调整：** 将其标记为“符号执行的性能瓶颈案例”，作为与静态工具（Slither 瞬间秒杀）对比的典型证据。

### **T3: Access Control (访问控制)**

- **合约：** `parity_wallet_bug_1.sol`。

- **失败原因：** **环境兼容性死锁。** 合约要求 0.4.9，但工具链最低仅支持 0.4.11。

- **解决方案：** 记录为“不可逾越的环境限制”，证明安全分析工具对早期远古代码的覆盖范围存在物理瓶颈。

  

- **合约：** `parity_wallet_bug_2.sol`。

- **失败原因：** **深度盲区。** 默认深度 12 无法触达复杂的 `delegatecall` 逻辑，导致“假阴性”（报告 Success 但无漏洞）。

- **解决方案：** 实施 **深度探测模式**，将参数优化为 `--max-depth 20` 并放宽超时至 10 分钟，成功捕获漏洞。

- 

- **合约：** `unprotected0.sol`。

- **失败原因：** **检测逻辑偏差。** Mythril 认为该权限篡夺后无法导致资金损失或合约自毁，判定其“无攻击价值”。

- **解决方案：** 在报告中明确区分“模式匹配（Slither）”与“逻辑证明（Mythril）”的差异，指出 Mythril 专注于“可利用性”而非“代码规范性”。



- **合约：** `phishable.sol / rubixi.sol`。
- **失败原因：** **语法代差导致的静态解析崩溃**。`rubixi.sol` 包含极其古老的构造函数写法（与合约名同名），且存在 `constant` 修饰函数的旧语法。0.8.x 编译器遇到这些标记会直接报错。
- **解决方案：** 同样通过 `--solv 0.4.25` 解决编译死锁。

### **T4: Unchecked Calls (未检查调用)**

- **合约：** `etherpot_lotto.sol` 。

- **失败原因：** **特征识别噪声。** Mythril 被大量的整数溢出报警（SWC-101）掩盖了真正的未检查调用漏洞。

- **解决方案：** 增加对 `--solv` 参数的规范使用，确保编译成功后，通过过滤 JSON 结果中的 `swc-104` 标签来精准定位目标漏洞。

  

- **合约：** `king_of_the_ether_throne.sol` 。

- **失败原因：** **极简语法不兼容**。这些 0.4.0 时代的合约，其 `address` 类型的权重逻辑和 `mapping` 声明方式与 0.8.x 严重冲突，导致编译器解析失败。

- **解决方案：** 显式指定旧版本编译器。在修复编译后，Mythril 展现了其优势：它不仅能发现 `SWC-104`（未检查返回值），还能同时识别出相关的 `SWC-101`（溢出），尽管这在本次实验中被视为“干扰噪声”。



- **合约：** `mishandled.sol` 。

- **失败原因：** **底层调用显式转换错误**。在 0.8.x 环境下，底层 `.call` 或 `.send` 的语法要求更严格，过时的写法导致编译器无法生成字节码。

- **解决方案：** 降级编译器。修复后，该合约在 **8.9秒** 内即被识别。结论：**对于特征明显的底层调用风险，符号执行在编译通过后的检测效率极高。**

  

### **T5 & T6: Oracle & Business Logic (复杂逻辑)**

- **合约：** 所有以 `_exp.sol` 结尾的复杂协议。

- **失败原因：** **项目工程依赖缺失。** 脚本无法识别 Foundry/Hardhat 的文件映射（如 `import "forge-std/Test.sol"`），导致编译级致命错误。

- **解决方案：** 1.  **降级实验：** 放弃对复杂框架项目的整体扫描。

  \2.  **结论沉淀：** 总结出“符号执行工具对工程化、模块化 DeFi 协议的静态预处理支持不足”的实验结论。